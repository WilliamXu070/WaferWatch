# Bend geometry

The active generalized-Euler construction is currently contained in
`bend/fdtd_lumopt/optimize.py`. Shared geometry should move here only after
standalone geometry tests demonstrate identical centerlines and polygons.
