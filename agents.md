# Agent Workflow Notes

## Required verification after coding changes

After every coding change, run both:

```bash
npm run lint
npm run build
```

This ensures the code is both lint-clean and production-build safe before we move on.
