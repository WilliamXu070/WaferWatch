# Archive

These files are preserved for provenance but are not active entry points.

- `william_code/`: the previous `William Code` implementation and its local
  outputs.
- `previous_attempts/root_bent_waveguide/`: the former repository-root bend
  pipeline.
- `previous_attempts/why_me/`: settings retained from the former `why me`
  workspace.

Do not import archive modules into the active workflow. Recover useful logic by
copying it into the appropriate active module with an explicit review.
