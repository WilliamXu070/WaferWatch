# Polarization rotator

- `reference/` contains the formerly tracked root reference scripts.
- `variants/` contains later experimental copies, including the preserved
  `og/` variant.

These files are references for coding conventions and are not part of the
active bend optimizer.
